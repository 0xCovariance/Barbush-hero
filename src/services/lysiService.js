// Phase 3 stub. Lysi is the AI practice companion. In Phase 3 this will call
// the Anthropic API (claude-sonnet) with the user's session history and weekly
// plan, returning either a conversational message or a structured action card.

export async function query(/* { prompt, context } */) {
  // Phase 3: implement Anthropic API call (claude-sonnet) with system prompt
  // defining Lysi's persona + structured-action-card response schema. Integrate
  // with Open Claw agent framework for music-theory / song recommendation tools.
  return null;
}

export function isLysiAvailable() {
  return false;
}
